import React from "react";

import "../assets/scss/header.scss";

export default function Header() {
    return (
        <header className="app-header">
            <a href="/">Modifier votre date de naissance</a>
        </header>
    );
}
